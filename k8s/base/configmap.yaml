apiVersion: v1
kind: ConfigMap
metadata:
  name: video-platform-config
  namespace: video-platform
data:
  # PostgreSQL
  POSTGRES_DB: "video_platform"
  POSTGRES_USER: "video"

  # Paths
  CLIPS_PATH: "/data/clips"

  # Server
  GIN_MODE: "release"
  SERVER_PORT: "8080"
