apiVersion: v1
kind: Secret
metadata:
  name: video-platform-secrets
  namespace: video-platform
type: Opaque
stringData:
  # PostgreSQL password - CHANGE IN PRODUCTION
  POSTGRES_PASSWORD: "changeme-in-production"

  # Database URL constructed from above
  DATABASE_URL: "postgres://video:changeme-in-production@postgres:5432/video_platform?sslmode=disable"
